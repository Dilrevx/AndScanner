# PatchSher

Mr.SherLock focused on searching patch status in the android firmware.  

> Note! You need to download binutils source code and compile it for arm objdump.

``` bash

```

``` bash
pip install -r requirements.txt
python main.py
```